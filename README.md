# https-github.com-chaya-chai-ProgrammingAssignment2
asgnmt 
makeCache matrix <- function (x= matrix ()) {

} inverse function undoes a function of the previous one

CacheSolve <- function< (x,...) ##returns an inverse of "x"
